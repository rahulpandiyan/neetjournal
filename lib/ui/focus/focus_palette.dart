import 'package:flutter/material.dart';

/// Immersive dark palette for the focus flow (locked to the app identity:
/// green ink, sage leaf green, warm gold energy). Shared by the focus screen
/// and its bottom sheets so every surface stays on-theme.
class FocusPalette {
  static const ink = Color(0xFF0E130E);
  static const panel = Color(0xFF1D291E);
  static const leaf = Color(0xFF8FD592);
  static const greenSoft = Color(0xFF243A26);
  static const amber = Color(0xFFFFC107);
  static const redSoft = Color(0xFFFFB4AB);
  static const text = Colors.white;
  static const textSoft = Color(0xFFA9B8AB);
  static const textDim = Color(0xFF6B7A6F);
  static const track = Color(0x1AFFFFFF);
}
